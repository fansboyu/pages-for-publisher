import { Body, Controller, Delete, Get, Headers, HttpCode, HttpStatus, Param, Post, Put, Query, UploadedFile, UseInterceptors } from '@nestjs/common'
import { FileInterceptor } from '@nestjs/platform-express'
import { ArticleDto, BootstrapDto, CategoryDto, CreateClientDto, CreateSiteDto, ExternalWebhookDto, LoginDto, UpdateSiteDto } from './dto'
import { ConfigService } from '@nestjs/config'
import { randomBytes } from 'crypto'
import { mkdir, writeFile } from 'fs/promises'
import { join } from 'path'
import { AccessService, AdminService, AuthService, ContentService } from './services'
import { User } from './entities'
const bearer=(h?:string)=>h

@Controller() export class HealthController { @Get('/health') health(){return {status:'ok'} } }
@Controller('/auth') export class AuthController { constructor(private auth:AuthService){} @Post('/bootstrap') bootstrap(@Body() dto:BootstrapDto){return this.auth.bootstrap(dto)} @Post('/login') login(@Body() dto:LoginDto){return this.auth.login(dto)} @Post('/refresh') refresh(@Body('refresh_token') token:string){return this.auth.refresh(token)} @Post('/logout') logout(@Body('refresh_token') token?:string){return this.auth.logout(token)} @Get('/me') async me(@Headers('authorization') h?:string){const u=await this.auth.userFromHeader(bearer(h));return {id:u.id,username:u.username,display_name:u.displayName,role:u.role,client_id:u.clientId} } }
@Controller('/admin/v1') export class AdminController { constructor(private auth:AuthService,private access:AccessService,private admin:AdminService){} private async user(h?:string){const u=await this.auth.userFromHeader(h);this.access.assertAdmin(u);return u} @Get('/dashboard') async dashboard(@Headers('authorization') h?:string){await this.user(h);return this.admin.dashboard()} @Get('/clients') async clients(@Headers('authorization') h?:string){await this.user(h);return this.admin.listClients()} @Post('/clients') async client(@Headers('authorization') h:string|undefined,@Body() dto:CreateClientDto){await this.user(h);return this.admin.createClient(dto)} @Post('/sites') async site(@Headers('authorization') h:string|undefined,@Body() dto:CreateSiteDto){await this.user(h);return this.admin.createSite(dto)} @Put('/sites/:siteId') async updateSite(@Headers('authorization') h:string|undefined,@Param('siteId') id:string,@Body() dto:UpdateSiteDto){await this.user(h);return this.admin.updateSite(id,dto)} }
@Controller('/portal/v1') export class PortalController { constructor(private auth:AuthService,private access:AccessService,private content:ContentService,private config:ConfigService){} private async context(h?:string){return this.auth.userFromHeader(h)} private async site(h:string|undefined,id:string){const u=await this.context(h),site=await this.content.site(id);this.access.assertClientSite(u,site);return site} @Get('/sites') async sites(@Headers('authorization') h?:string){return this.content.sitesFor(await this.context(h))} @Get('/sites/:siteId/articles') async articles(@Headers('authorization') h:string|undefined,@Param('siteId') id:string,@Query('status') status?:any){await this.site(h,id);return this.content.listArticles(id,status)} @Get('/sites/:siteId/categories') async categories(@Headers('authorization') h:string|undefined,@Param('siteId') id:string){await this.site(h,id);return this.content.listCategories(id)} @Post('/sites/:siteId/categories') async addCategory(@Headers('authorization') h:string|undefined,@Param('siteId') id:string,@Body() dto:CategoryDto){return this.content.addCategory(await this.site(h,id),dto)} @Delete('/sites/:siteId/categories/:categoryId') async removeCategory(@Headers('authorization') h:string|undefined,@Param('siteId') id:string,@Param('categoryId') categoryId:string){return this.content.removeCategory(await this.site(h,id),categoryId)} @Post('/sites/:siteId/articles') @HttpCode(HttpStatus.ACCEPTED) async create(@Headers('authorization') h:string|undefined,@Param('siteId') id:string,@Body() dto:ArticleDto){return this.content.saveArticle(await this.site(h,id),dto)} @Put('/sites/:siteId/articles/:articleId') @HttpCode(HttpStatus.ACCEPTED) async update(@Headers('authorization') h:string|undefined,@Param('siteId') id:string,@Param('articleId') articleId:string,@Body() dto:ArticleDto){return this.content.saveArticle(await this.site(h,id),dto,articleId)} @Delete('/sites/:siteId/articles/:articleId') @HttpCode(HttpStatus.ACCEPTED) async remove(@Headers('authorization') h:string|undefined,@Param('siteId') id:string,@Param('articleId') articleId:string){return this.content.deleteArticle(await this.site(h,id),articleId)} @Post('/sites/:siteId/articles/:articleId/restore') async restore(@Headers('authorization') h:string|undefined,@Param('siteId') id:string,@Param('articleId') articleId:string){return this.content.restoreArticle(await this.site(h,id),articleId)} @Get('/sites/:siteId/jobs') async jobs(@Headers('authorization') h:string|undefined,@Param('siteId') id:string){await this.site(h,id);return this.content.listJobs(id)} @Post('/uploads/images') @UseInterceptors(FileInterceptor('file',{limits:{fileSize:5*1024*1024}})) async upload(@Headers('authorization') h:string|undefined,@UploadedFile() file:any){await this.context(h);if(!file||!['image/jpeg','image/png','image/webp','image/gif'].includes(file.mimetype))throw new Error('only jpg, png, webp or gif images are allowed');const extension:Record<string,string>={"image/jpeg":"jpg","image/png":"png","image/webp":"webp","image/gif":"gif"};const root=this.config.get('UPLOAD_ROOT')??'/data/uploads';await mkdir(root,{recursive:true});const name=`${Date.now()}-${randomBytes(8).toString('hex')}.${extension[file.mimetype]}`;await writeFile(join(root,name),file.buffer);return {url:`${this.config.get('PUBLIC_API_BASE_URL')??'https://api-v2.clickadwave.org'}/uploads/${name}`}} }
@Controller('/webhooks/qiyouling') export class WebhookController { constructor(private content:ContentService){} @Post('/:domain/articles') @HttpCode(HttpStatus.ACCEPTED) publish(@Param('domain') domain:string,@Body() dto:ExternalWebhookDto){return this.content.ingestWebhook(domain,dto)} }
